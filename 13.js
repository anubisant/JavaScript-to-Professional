// 13) Програмыг debug хийх буюу алхам алхмаар ажиллуулж шалгах

// Install to NodeJS

var avgDun = 50;

switch (true) {
  case avgDun >= 90:
    console.log(name + " A");
    break;
  case avgDun < 90 && avgDun >= 80:
    console.log(name + " B");
    break;
  case avgDun < 80 && avgDun >= 70:
    console.log(name + " C");
    break;
  default:
    console.log(name + " D");
}
